
import React from 'react';
import { CardFooter } from "@/components/ui/card";

const TestModeFooter: React.FC = () => {
  // This component is now empty as we're removing the Test Mode notification
  return null;
};

export default TestModeFooter;
